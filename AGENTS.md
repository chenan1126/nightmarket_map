# 夜市地圖專案協作規範

## 專案目標

本專案建立台灣夜市與攤位的可查詢地圖，讓使用者先找到住家或目前位置附近的夜市，再透過社群共同補充與複核資料。第一階段以中壢夜市與中原夜市試行，確認流程穩定後再逐步擴充至全台夜市。

## 資料來源與待複核原則

- 新增夜市、攤位或重要欄位時，應保留可追溯的資料來源網址或文件說明。
- 社群提交的內容先進入待複核狀態，不得直接視為正式資料。
- 優先採用政府開放資料、夜市或商圈官方資訊、店家官方頁面等一手來源；來源衝突時保留證據並標記需要補證據。
- 不確定的營業時間、位置、店名或是否營業，應標記不確定性，不可自行臆測填入。
- 匿名投稿可以提供資料，但正式採用前仍須經管理審核與社群流程。

## Supabase、RLS 與安全規則

- Supabase 是正式資料與身份驗證來源；資料表的權限以 Row Level Security（RLS）為準，不可只依賴前端隱藏按鈕。
- 公開讀取僅限允許公開的夜市、攤位、提案狀態與彙整結果；投稿者身份欄位不得不必要地公開。
- 匿名使用者可提交有資料來源的提案，但不可投票或評星；匿名投稿須受資料庫端的頻率限制。
- 會員登入後才能參與新增資料的支持、反對或補證據表決；同一會員對同一提案只能保留一票，改票應更新原票。
- Turnstile 只用於降低機器濫用；前端只放公開 Site Key，Secret Key 只能設定在 Supabase 或受保護的伺服器環境。
- 不得把 `.env`、`.env.local`、Supabase Secret、Service Role Key、Turnstile Secret、OAuth secret、token 或其他憑證提交到 Git。
- 任何涉及放寬資料存取權、修改 RLS、啟用匿名登入或變更生產環境設定的操作，都要先說明影響並取得專案負責人確認。

## 社群表決與星評分離

- 「是否採用新增提案」的支持、反對、需補證據表決，與使用者對已採用攤位的 1–5 星喜好評分是兩套不同功能。
- 星星分數不能直接決定資料是否進入正式名錄，也不能被付費曝光或管理操作任意改寫。
- 付費功能若日後加入，只能提供曝光、商家頁面工具、優惠或分析服務；贊助內容須清楚標示，不得購買好評或表決結果。

## 開發命令與驗證

在專案根目錄執行：

```bash
npm install
npm run dev
npm run build
```

- 提交前至少執行 `npm run build`，確認前端可正常打包。
- 涉及 Supabase schema 或 RLS 的變更，應使用版本化 migration，並檢查遠端 migration 狀態與必要的讀寫權限。
- 涉及登入、匿名投稿、表決或星評分的變更，應驗證成功流程、權限拒絕流程，以及重複操作不會產生重複票或繞過限制。
- 以小範圍、可回溯的提交維持變更；不要為了方便重設、覆蓋或刪除其他人的未提交工作。

## 協作方式

- 本專案的程式實踐由 Luna 負責；產品方向、資料規則、驗收條件與風險由 PM 先說明清楚。
- 修改前先閱讀相關程式與 migration，避免用猜測覆蓋既有行為。
- 每次變更只處理一個清楚的目標，回報修改內容、驗證方式、尚未驗證的部分與任何需要人工操作的步驟。
- 需要瀏覽器、Supabase Dashboard、Cloudflare 或信箱操作時，先告知使用者，由使用者親自操作；不要自行操作瀏覽器或要求使用者提供 secret。
