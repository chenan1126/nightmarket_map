# 夜市社群探索體驗改版 Goal

## 目標

讓第一次打開網站的人能在幾秒內找到附近或想去的夜市，進入每個夜市自己的社群頁；讓貢獻者容易找到可補充的資料。視覺參考 [去趣首頁](https://www.chictrip.com.tw/landing) 的探索節奏與清楚層級，保留夜市社群自己的識別、圖片與文案。

## 參考網站的設計觀察（2026-09-19）

- 白色單列導覽，主要行動集中且不擁擠。
- 寬幅彩色主視覺先吸引目光；淺色背景把搜尋區與內容區連起來。
- 搜尋面板是第一個清楚的操作焦點，分類捷徑直接排列在其下。
- 內容區先以大圖和不等寬拼貼呈現探索題材，再提供地區與分類篩選；讀者不用先讀大量說明。
- 圖片、短標題與可點擊入口共同引導瀏覽，較長的文章與資料留到下一層頁面。

## 夜市產品的轉譯

1. **首頁**：保留一張自有夜市意象主視覺；搜尋、定位、縣市篩選與快捷入口集中在一個探索區。下方以圖片帶出中壢／中原試行與附近夜市，社群提案另成一個清楚區段。完整名錄與地圖是次級探索入口。
2. **夜市專頁**：`/markets/:externalId` 是可直接分享與重新整理的頁面。首屏先交代夜市名稱、地區、已核對資訊與參與入口；攤位、提案、來源與回報分層呈現。不可退回彈窗詳情。
3. **社群流程**：匿名投稿需資料來源；登入後才能表決與星評。入口與狀態要容易理解，使用者不必閱讀後台式長表格才知道如何參與。
4. **視覺識別**：借鏡清爽留白、親切圖像與明確操作層級，使用夜市專屬色彩和自製圖片。生成圖一律標示為意象圖，不得冒稱中壢或中原實景。圖片不得與去趣資產或品牌圖像相同。

## 驗收清單

- [ ] 首頁桌面與手機版有一致的探索層級：主視覺、搜尋／分類、圖片導覽、社群動態、次要工具。
- [ ] 搜尋、定位、縣市、全台名錄、社群提案捷徑都有實際行為；空結果有明確提示。
- [ ] 中壢觀光夜市與中原夜市可從首頁進入各自專頁，直接開啟其 URL 與重新整理仍正常。
- [ ] 夜市專頁能在不打開長彈窗的情況下查看地址、來源、攤位、提案、星評與資料回報。
- [ ] 首頁與夜市專頁使用同一套文字、色彩、間距、圓角、按鈕與空狀態規則；不再以重複卡片堆疊主要內容。
- [ ] 自製圖片提供適合桌面與手機的裁切，標示意象圖，並控制下載大小。
- [ ] 在 320px、手機常見寬度及桌面寬度沒有水平溢出，文字與按鈕可讀、可點擊。
- [ ] `npm run build` 通過；登入、投稿、表決、星評、回報流程沒有因介面改版中斷。
- [ ] 完成實際瀏覽器視覺與功能驗收，再推送公開 GitHub。

## 目前實作與待驗收（2026-09-19）

- 首頁已加入夜市主視覺、集中搜尋與捷徑、圖片導覽；中壢與中原入口連到獨立專頁，名錄與地圖入口改為次級內容。
- 夜市專頁已分開呈現基本資訊、來源、攤位提案、表決、星評與回報；圖片都標示為意象圖。新圖檔各約 370 KB。
- 舊夜市詳情彈窗已移除，首頁夜市入口改為連往 `/markets/:externalId`。
- 夜市專頁的未登入表決入口、五顆星評分、資料回報與表單錯誤訊息已做就近顯示；實際送出及鍵盤操作仍待驗收。
- 地圖已改成展開次級入口時才載入；初始主程式由約 654 KB 降為約 500 KB（未壓縮），地圖點位與展開後互動仍待瀏覽器驗收。
- `npm run build` 與 `git diff --check` 已通過。仍需由專案負責人在瀏覽器檢查桌面、320px 手機版，以及登入／投稿／表決／星評／回報的實際流程。

## 實作順序

1. 整理目前首頁與夜市頁的資訊架構、樣式覆寫與可重用元件。
2. 完成首頁圖片導覽及分類篩選，移除重複控制。
3. 完成夜市專頁的社群資訊層級與響應式版面。
4. 統一投稿、登入、表決與回報的外觀及錯誤／空狀態。
5. 驗收桌面、手機與關鍵互動，修正後提交與推送。
